import React from 'react';
import Hero from '../../components/Hero';
import Projects from '../../components/Projects';
import Contact from '../../components/Contact';

export default function Home() {
  return (
    <main>
      <Hero />
      <Projects />
      <Contact />
    </main>
  )
}
// This is a sample Next.js application with Tailwind CSS.
// It includes a main page with a heading and a paragraph.
// The page is styled using Tailwind CSS classes.
// The heading is a large, bold, underlined text.
// The paragraph is a smaller, regular text.
// The main function is the default export of the module.
// The main function returns a JSX element that represents the main page of the application.
// The main function is the entry point of the application.
// The main function is called when the application is run. 